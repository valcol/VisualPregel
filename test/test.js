'use strict';

let assert = require('assert');

describe('#funtionToTest()', function () {
    it('should return something', function () {
      let something = true;
      assert.ok(something, 'OK');
    });
});
